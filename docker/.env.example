# Configuración para docker/startup.sh
# Rutas a proyectos Docker (separadas por espacios)
PROJECT_PATHS="/ruta/proyecto1 /ruta/proyecto2"

# Mapeos FQDN (formato: fqdn1,contenedor1;fqdn2,contenedor2,...)
# Nota: Los mapeos múltiples se separan por punto y comas sin espacios
FQDN_MAPPINGS="api.midominio.com,backend;db.midominio.com,database"

# Control de mapeo local (true/false)
# true: deshabilita mapeo local, solo aplica FQDN
# false: habilita mapeo local (valor predeterminado)
NO_LOCAL_MAP=true